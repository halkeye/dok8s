---
ingress:
  enabled: true
  hosts:
    - vouch.g4v.dev
  tls:
    - secretName: vouch-tls
      hosts:
        - vouch.g4v.dev
  annotations:
    kubernetes.io/tls-acme: "true"

config:
  vouch:
    allowAllUsers: false
    domains:
      - g4v.dev
    webapp: true
    testing: false
  oauth:
    provider: google
